Create or update mock data for: $ARGUMENTS

1. Read `src/data/CLAUDE.md` for the data schema and naming conventions
2. Check if the JSON file already exists in `src/data/`
3. Create realistic Thai data matching the อพวช. museum context
4. Use the exact project/user names from the demo dataset
5. Store dates in ISO 8601 CE format (Buddhist Era conversion is done in UI)
6. Store currency as raw numbers in Thai Baht (formatting done in UI)
7. Create the corresponding API route in `src/app/api/` following the in-memory store pattern
8. Create the React Query hook in `src/hooks/`
9. Run `npm run typecheck` to verify types match

Reference the demo data in `src/data/CLAUDE.md` for consistency.
